services:
  mlforge:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8001:8001"
    volumes:
      - mlforge-data:/app/backend/data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

volumes:
  mlforge-data:
